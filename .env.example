# Local dev: docker compose up -d, then use this connection string as-is.
# Prod (Vercel): set to the Neon connection string (Project settings > Connection string).
DATABASE_URL="postgres://careerboard:careerboard@localhost:5433/careerboard"

# Generate with: npx auth secret
AUTH_SECRET=""
